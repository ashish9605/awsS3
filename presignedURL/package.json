{
  "name": "presignedurl",
  "version": "1.0.0",
  "description": "Fetch the presigned url from s3 and that url will be valid for 1 day",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ashish Shukla",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.740.0"
  }
}
